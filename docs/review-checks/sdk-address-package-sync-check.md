# SDK Address Package Sync Check

- Compare the production contract address with `@bamzzstudio/cforge-addresses`.
- Confirm the address package and `.env.example` point to the same mainnet contract.
- Verify preview deployments do not override the mainnet address by accident.
