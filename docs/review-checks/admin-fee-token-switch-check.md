# Admin Fee Token Switch Check

- Review the admin view after switching the configured fee token.
- Confirm labels, approval copy, and treasury balances use the same token.
- Record the checked token address in release notes.
