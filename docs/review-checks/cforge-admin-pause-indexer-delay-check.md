# C-forge Admin Pause Indexer Delay Check

Pause the contract while contract reads are slow to refresh.
Confirm C-forge keeps the pending transaction link visible.
Verify mint controls do not re-enable during the delay.
