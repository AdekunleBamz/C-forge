# Fee Amount Env Boundary Check

- Test fee amounts at zero, normal, and unusually large values in a preview.
- Confirm labels and transaction values match the configured base units.
- Verify unsafe fee values are caught before release.
