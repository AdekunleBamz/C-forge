# C-forge Wallet Chain Badge After Switch Check

Connect on an unsupported chain and switch back to Celo.
Confirm the chain badge updates without a full page refresh.
Verify mint and admin actions unlock only after the supported chain is active.
