# Fee Token Fallback Check

Unset the preferred fee token locally and confirm fallback token copy is clear.
Record the environment variable state and resulting token label.
Restore production-like settings before release testing.
