# C-forge Proof Share Native Fallback Check

Use proof sharing in a browser without native share support.
Confirm C-forge falls back to copying the proof link.
Verify feedback distinguishes copy fallback from native sharing.
