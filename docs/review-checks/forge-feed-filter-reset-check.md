# Forge Feed Filter Reset Check

- Apply proof feed filters, then clear them.
- Confirm all proofs return without requiring a page reload.
- Verify the empty-filter state is distinct from an empty gallery.
