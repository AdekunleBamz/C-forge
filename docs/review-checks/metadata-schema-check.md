# Metadata Schema Check

Validate minted metadata includes `name`, `description`, `image`, and proof attributes.
Record the token id and metadata URL checked.
Flag missing fields before production promotion.
