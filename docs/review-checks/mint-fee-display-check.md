# Mint Fee Display Check

Compare the displayed mint fee with configured native and stable fee values.
Record the token symbol, formatted amount, and environment variable source.
Flag any mismatch before release.
