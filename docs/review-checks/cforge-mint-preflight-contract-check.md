# C-forge Mint Preflight Contract Check

Run mint setup with the C-forge token contract address missing.
Confirm preflight blocks minting before any wallet prompt.
Verify the message points to contract configuration rather than user funds.
