# C-forge Proof Metadata Special Character Check

Create proof metadata with punctuation and accented words.
Confirm preview and uploaded JSON encode the values consistently.
Verify the metadata remains valid JSON after upload.
